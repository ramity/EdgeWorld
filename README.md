# Setup:

```bash
# --------------------------------------------
# Verbose steps:
# ---------------------------------------------

# clone repo
git clone https://github.com/ramity/EdgeWorld

# start server (starting from root dir of repo)
cd server && bin/console server:run

# start client (starting from root dir of repo)
cd client && npm install && npm start
```
